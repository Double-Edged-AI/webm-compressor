# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# PyInstaller output
build/
dist/

# FFmpeg binaries — never commit (large + separately licensed; fetched on first run)
ffmpeg.exe
ffprobe.exe
ffmpeg
ffprobe

# Env / secrets — never commit
.env
.env.*
*.pem
*.key
secrets.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Test / temp output
*.webm
*.log
